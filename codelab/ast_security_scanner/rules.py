"""
rules.py

이 파일은 보안 검사 규칙(rule)을 모아두는 파일이다.

이번 과제의 핵심은 AST로 함수 호출을 찾아낸 뒤,
그 함수가 "위험 함수 목록"에 포함되는지 확인하는 것이다.

따라서 위험 함수 목록을 별도의 파일로 분리해 두면 장점이 있다.

1. 분석 로직(scanner.py)과 규칙 데이터가 분리된다.
2. 나중에 새로운 위험 함수를 추가하기 쉽다.
3. 유지보수가 쉬워진다.
4. 실무에서도 이런 식으로 "정책"과 "로직"을 분리하는 경우가 많다.

딕셔너리 형태로 구성한 이유:
- key   : 탐지할 함수 이름
- value : 왜 위험한지에 대한 설명

예:
    "eval": "문자열을 코드로 실행하므로 코드 인젝션 위험"

이렇게 두면 탐지 시 key로 빠르게 찾고,
보고서에는 value를 설명으로 바로 출력할 수 있다.
"""

# -------------------------------------------------------------------
# 위험 함수 목록
# -------------------------------------------------------------------
# 이번 과제 설명에 포함된 대표적인 위험 함수와,
# 실무에서도 자주 제한되는 몇 가지 함수를 함께 넣어두었다.
#
# 필요하다면 여기에 규칙을 계속 추가하면 된다.
# 예를 들어:
#   "yaml.load": "안전하지 않은 역직렬화 가능성"
# 같은 항목도 나중에 쉽게 추가할 수 있다.
# -------------------------------------------------------------------
RISKY_FUNCTIONS = {
    "eval": "문자열을 코드로 실행하므로 코드 인젝션 위험",
    "exec": "동적으로 코드를 실행하므로 매우 위험함",
    "pickle.load": "역직렬화 과정에서 악성 객체 실행 위험이 있음",
    "pickle.loads": "역직렬화 과정에서 악성 객체 실행 위험이 있음",
    "os.system": "쉘 명령 실행으로 인해 command injection 위험이 있음",
    "subprocess.Popen": "외부 프로세스 실행. 인자 검증이 없으면 위험할 수 있음",
    "subprocess.call": "외부 명령 실행. 잘못 사용 시 명령어 삽입 위험이 있음",
    "subprocess.run": "외부 명령 실행. 특히 shell=True와 함께 사용 시 위험함",
}